# ── LangChain ─────────────────────────────────────────────────────
langchain
langchain-community
langchain-classic
langchain-core
langchain-openai
langchain-text-splitters
langchain-experimental          # SemanticChunker
langchain-chroma
langchain-qdrant

# ── LLM Providers ─────────────────────────────────────────────────
openai

# ── Embeddings & Vector Store ──────────────────────────────────────
sentence-transformers           # HuggingFaceCrossEncoder backend
chromadb

# ── Document Loading ───────────────────────────────────────────────
pypdf
pymupdf                         # PDF page rendering for evidence highlighting
requests                        # URL-based PDF download

# ── LandingAI ADE ─────────────────────────────────────────────────
landingai-ade

# ── Retrieval ─────────────────────────────────────────────────────
rank-bm25                       # BM25Retriever backend

# ── Reranker ──────────────────────────────────────────────────────
flashrank

# ── Guardrails ────────────────────────────────────────────────────
# Optional — only needed when ENABLE_INPUT_GUARDRAILS or ENABLE_OUTPUT_GUARDRAILS = True
# Requires manual setup: guardrails configure && guardrails hub install hub://...
# guardrails-ai>=0.5.13

# ── Evaluation ────────────────────────────────────────────────────
ragas>=0.2.0
datasets                        # ragas internal dependency
deepeval
json-repair

# ── Reporting ─────────────────────────────────────────────────────
reportlab
matplotlib
numpy
pandas

# ── API ───────────────────────────────────────────────────────────
fastapi
uvicorn[standard]               # ASGI server with uvloop + httptools
python-multipart                # required for UploadFile (file ingestion endpoint)

# ── Frontend ──────────────────────────────────────────────────────
streamlit

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv
langsmith                       # LangSmith tracing for LangChain pipelines
python-json-logger             # JSON structured log formatting
prometheus-fastapi-instrumentator  # Prometheus /metrics endpoint + HTTP auto-instrumentation
